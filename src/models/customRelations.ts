//Do Not Delete this file
import { Sequelize } from "sequelize";

/** 
 * This establishes the other custom defined relationships
 * which are beyond the scope of auto generation
 * @param _sequelize Sequelize
*/
export function establishCustomRelations(_sequelize: Sequelize) {
  // Intentionally left empty.
  // Add custom Sequelize model relationships here if needed in the future.
}